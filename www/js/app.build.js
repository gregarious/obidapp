// currently only handles the app Javascript directory
({
    appDir: "../",
    baseUrl: "js",
    dir: "../../deploy/www",

    paths: {
        templates: "../templates"
    },

    modules: [
        {
            name: "main"
        }
    ]
})